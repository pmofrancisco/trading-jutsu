name: tests-journals-service

on:
  pull_request:
    paths:
      - journals-service/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd journals-service && npm install && npm run test:ci
